services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: saju
      POSTGRES_USER: saju_user
      POSTGRES_PASSWORD: localpassword
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
